version: "3.9"
services:
  postgres:
    image: postgres:latest
    container_name: telros_test_task
    environment:
      POSTGRES_DB: "users"
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "root"
    ports:
      - "5432:5432"